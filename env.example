# ==========================================
# RAG Chatbot API - Environment Configuration
# ==========================================
#
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# NEVER commit .env to version control!
# ==========================================

# ---------- OpenAI API (Optional) ----------
# Leave empty to use free HuggingFace models
# Get API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=

# OpenAI model to use (if API key is set)
# Options: gpt-3.5-turbo, gpt-4, gpt-4-turbo
OPENAI_MODEL=gpt-3.5-turbo

# ---------- Embedding Model ----------
# HuggingFace model for document embeddings
# Options:
#   - sentence-transformers/all-MiniLM-L6-v2 (fast, good quality)
#   - sentence-transformers/all-mpnet-base-v2 (slower, better quality)
EMBEDDING_MODEL=sentence-transformers/all-MiniLM-L6-v2

# ---------- Text Chunking ----------
# Size of text chunks for splitting documents
# Smaller = more granular, larger = more context
CHUNK_SIZE=500

# Overlap between chunks to maintain context
CHUNK_OVERLAP=50

# ---------- Retrieval Settings ----------
# Number of chunks to retrieve for each question
# More = better context, but slower
RETRIEVAL_K=3

# Search type for vector store
# Options: similarity, mmr (maximum marginal relevance)
SEARCH_TYPE=similarity

# ---------- LLM Settings (HuggingFace) ----------
# Model to use when OpenAI API key is not set
# Options:
#   - google/flan-t5-base (fast, small)
#   - google/flan-t5-large (balanced)
#   - google/flan-t5-xl (best quality, slow)
LLM_MODEL=google/flan-t5-base

# Maximum length for LLM generation
LLM_MAX_LENGTH=512

# Temperature for LLM (0.0 = deterministic, 1.0 = creative)
LLM_TEMPERATURE=0.7

# ==========================================
# Example Configurations:
# ==========================================

# Fast & Free (Default):
# - Uses HuggingFace models
# - No API costs
# - Good for development/testing
# OPENAI_API_KEY=
# LLM_MODEL=google/flan-t5-base

# High Quality (Paid):
# - Uses OpenAI GPT models
# - Better answers
# - API costs apply
# OPENAI_API_KEY=sk-your-actual-key-here
# OPENAI_MODEL=gpt-4

# Balanced (Free but Slower):
# - Uses larger HuggingFace model
# - Better than base, free
# - Requires more memory
# OPENAI_API_KEY=
# LLM_MODEL=google/flan-t5-large

# ==========================================
